version: 2.1

orbs:
  win: circleci/windows@2.2.0

jobs:
  build_backend:
    working_directory: ~/project/Dort
    steps:
      - checkout
      - run: dotnet build

  build_web:
    working_directory: ~/project/web
    docker:
      - image: circleci/node:10-browsers
    steps:
      - checkout
      - restore_cache:
          key: AngularCircleCI-{{ .Branch }}-{{ checksum "package.json" }}
      - run: npm install
      - save_cache:
          key: AngularCircleCI-{{ .Branch }}-{{ checksum "package.json" }}
          paths:
            - "node_modules"
      - run: npm run ng build

workflows:
  build_api:
    jobs:
      - build_backend
  build_web:
    jobs:
      - build_web
